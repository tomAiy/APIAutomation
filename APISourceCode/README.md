# API Testing

This test comes with a mock API server that represents a snapshot of a REST API.

## Installation
### Pre-requisites
* LTS version of NodeJS, and NPM: https://nodejs.org/

### Usage
Start the mock API server:
`npm install ; npm run start`

It runs by default on `http://localhost:8080`
